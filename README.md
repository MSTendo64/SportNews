# 🏆 Спорт Новости - Flask сайт

Полнофункциональный сайт спортивных новостей на Flask с современным дизайном и всеми необходимыми функциями.

## 📋 Функциональные возможности

- ✅ Главная страница с рекомендуемыми новостями
- ✅ Детальный просмотр новостей с подсчетом просмотров
- ✅ Категории новостей (Футбол, Хоккей, Баскетбол, и т.д.)
- ✅ Поиск по новостям
- ✅ Страница "О нас"
- ✅ Адаптивный дизайн (работает на мобильных устройствах)
- ✅ SQLite база данных
- ✅ Пагинация на страницах категорий
- ✅ Социальные кнопки
- ✅ Статистика просмотров

## 🚀 Быстрый старт

### 1. Клонирование и установка

```bash
# Клонируйте проект
git clone <ваш-репозиторий>
cd sport-news-site

# Создайте виртуальное окружение (рекомендуется)
python -m venv venv

# Активируйте виртуальное окружение
# На Windows:
venv\Scripts\activate
# На macOS/Linux:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Запуск приложения

```bash
# Запустите сервер Flask
python app.py
```

### 3. Откройте в браузере

Перейдите по адресу: [http://localhost:5001](http://localhost:5001)

## 🗂️ Структура проекта

```
sport-news-site/
├── app.py              # Основное Flask приложение
├── config.py           # Конфигурация приложения
├── requirements.txt    # Зависимости Python
├── sport_news.db      # База данных SQLite (создается автоматически)
├── static/            # Статические файлы
│   └── css/
│       └── style.css  # Пользовательские стили
└── templates/         # HTML шаблоны
    ├── base.html      # Базовый шаблон
    ├── index.html     # Главная страница
    ├── news_detail.html # Детальная страница новости
    ├── category.html  # Страница категории
    ├── about.html     # Страница "О нас"
    └── search.html    # Страница поиска
```

## 🛠️ Технологии

- **Backend:**
  - Flask 2.3.3
  - Flask-SQLAlchemy 3.0.5
  - SQLite (легковесная БД)

- **Frontend:**
  - Bootstrap 5.1.3
  - Font Awesome 6.4.0
  - CSS3

- **База данных:**
  - SQLite с моделями:
    - News (новости)
    - Category (категории)

## 📊 Модели данных

### News (Новости)
- `id` - уникальный идентификатор
- `title` - заголовок новости
- `content` - полный текст новости
- `short_description` - краткое описание
- `image_url` - URL изображения
- `date_published` - дата публикации
- `views` - количество просмотров
- `is_featured` - является ли новость рекомендуемой
- `category_id` - связь с категорией

### Category (Категории)
- `id` - уникальный идентификатор
- `name` - название категории
- `slug` - URL-адрес категории

## 🔧 Настройка и конфигурация

### Настройки в config.py:
```python
NEWS_PER_PAGE = 6        # Новостей на страницу
SECRET_KEY = 'ваш-секретный-ключ'  # Для сессий Flask
```

### Создание собственных новостей:

1. **Через код Python:**
```python
from app import db, News, Category
# Добавьте новую категорию или новость в функции create_sample_data()
```

2. **Через SQLite браузер:**
   - Используйте программу типа DB Browser for SQLite
   - Откройте файл `sport_news.db`
   - Добавьте записи в таблицы `news` и `category`
